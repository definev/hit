name: hit
description: Decouple Flutter layout size from hit-test size, and deliver taps outside parent bounds.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0-282.1.beta
  flutter: ">=3.47.0-1.0.pre-10"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
